apiVersion: v1
kind: ResourceQuota
metadata:
  name: security-defectdojo
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 3Gi
    limits.cpu: "5"
    limits.memory: 5Gi
    requests.storage: 2Gi
    persistentvolumeclaims: "1"
    count/pods: "8"
    count/jobs.batch: "2"
