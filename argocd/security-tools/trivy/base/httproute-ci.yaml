apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: security-trivy-ci
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik
      namespace: traefik
      sectionName: websecure
  hostnames:
    - ci-trivy.no-name.win
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: security-trivy
          port: 4954
          weight: 1
