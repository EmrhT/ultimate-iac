apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: prometheus-daily-snapshot
  namespace: longhorn-system
spec:
  # 09:00 Europe/Istanbul (the cluster schedules these jobs in UTC).
  cron: "0 6 * * *"
  task: snapshot
  retain: 1
  concurrency: 1
