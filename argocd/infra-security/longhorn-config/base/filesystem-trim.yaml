apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: filesystem-trim
  namespace: longhorn-system
spec:
  cron: "0 3 * * 0"
  task: filesystem-trim
  groups:
    - default
  retain: 0
  concurrency: 1
